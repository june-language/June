#ifndef prompt_hpp
#define prompt_hpp

#include <string>
#include <iostream>
#include <cstdlib>

enum class Color {
  Red,
  Green,
  Yellow,
  Blue, 
  Magenta,
  Cyan,
  White,
  Reset
};

static inline char *ColorFor(Color color) {
  switch (color) {
    
  }
}

#endif
